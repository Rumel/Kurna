<UserControl x:Class="Kurna.Views.BoardPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:views="clr-namespace:Kurna.Views"
             mc:Ignorable="d" 
             d:DesignHeight="640" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="96" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <!-- Player One -->
            <TextBlock Grid.Column="0" Text="Player 1" FontSize="32" HorizontalAlignment="Center" VerticalAlignment="Center" FontWeight="SemiBold"/>
            <!-- Player Two -->
            <TextBlock Grid.Column="1" Text="Player 2" FontSize="32" HorizontalAlignment="Center" VerticalAlignment="Center" FontWeight="SemiBold"/>
        </Grid>
        <views:Board Grid.Row="1" Grid.Column="0">
        </views:Board>
    </Grid>
</UserControl>
